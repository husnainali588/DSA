#include <iostream>
#include <unordered_set>
using namespace std;

void longestSubstring (string s) {
    int n = s.length();
    unordered_set<char> st;

    for(int i=0; i<n; i++){
        if(st.find(s[i])!=st.end()){
            
        } else {

        }
    }
}

int main() {
    
    return 0;
}